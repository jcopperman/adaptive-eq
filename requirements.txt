spotipy>=2.19.0
# For Linux systems, pygobject needs to be installed via system packages
# sudo apt-get install python3-gi python3-gi-cairo gir1.2-gtk-3.0 gir1.2-appindicator3-0.1
# The following are used for building the AppImage
pyinstaller>=5.0.0
